func indexOf(str, search) {
    return 1
}

func readdir(path, ext) {
    list = array()
    files = call("ls", path)
    fliter = concat(".", ext)
    for f in files {
        if indexOf(f, fliter) > 0 {
            list = append(f)
        }
    }
    return list
}

println(readdir("./examples", "bs"))
